# 自主开发说明

`independent-rebuild` 分支从仓库导入 DramaClaw 之前的提交创建，并重新建立项目文件。

本分支不复制 DramaClaw 的源码、界面文件、数据结构或内部实现。允许吸收的仅是公开产品中常见的抽象思路，例如无限画布、资产库、任务中心、主线流程与自由探索结合。这些思路会通过本项目自己的领域模型、组件、API 和工作流重新实现。

在自主版本替换 `main` 前，应删除或归档当前导入快照相关分支和标签，避免许可证、品牌和代码来源混淆。
