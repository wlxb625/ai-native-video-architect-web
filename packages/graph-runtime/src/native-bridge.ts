import{pathToFileURL}from'node:url';import{resolve}from'node:path';import type{GraphIR,RunOptions,RunResult}from'./types.js';
export async function runNativeGraph(runtimePath:string,graph:GraphIR,input:Record<string,unknown>,options:RunOptions):Promise<RunResult>{const moduleUrl=pathToFileURL(resolve(runtimePath)).href;const runtime=await import(moduleUrl)as{runGraph:(graph:GraphIR,input:Record<string,unknown>,options:{nodeExecutors:RunOptions['nodeExecutors']})=>Promise<RunResult>};if(typeof runtime.runGraph!=='function')throw new Error('GraphEngineering runtime does not export runGraph');await options.onEvent?.({type:'runtime.native.selected',timestamp:new Date().toISOString()});return runtime.runGraph(graph,input,{nodeExecutors:options.nodeExecutors});}
