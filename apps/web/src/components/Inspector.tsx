import{WandSparkles}from'lucide-react';import type{Node}from'@xyflow/react';export function Inspector({node,onUpdate,onGenerate}:{node?:Node;onUpdate:(patch:Record<string,unknown>)=>void;onGenerate:()=>void}){if(!node)return <aside className="inspector"><div className="inspector-empty"><div className="empty-orb"/><h3>选择一个节点</h3><p>在这里编辑内容、查看上下文并触发后续生成。</p></div></aside>;return <aside className="inspector"><div className="inspector-head"><div><span>节点检查器</span><h2>{String(node.data.title??'未命名节点')}</h2></div><span className="pill">{node.type}</span></div><label>标题<input value={String(node.data.title??'')} onChange={e=>onUpdate({title:e.target.value})}/></label><label>摘要<textarea rows={5} value={String(node.data.summary??'')} onChange={e=>onUpdate({summary:e.target.value})}/></label><label>生成指令<textarea rows={7} value={String(node.data.prompt??'')} onChange={e=>onUpdate({prompt:e.target.value})} placeholder="描述下一步的画面、情绪、动作或信息目标。"/></label><button className="generate-button" onClick={onGenerate}><WandSparkles size={17}/><span>基于此节点生成下一步</span></button><div className="security-note"><strong>服务器持久化</strong><span>修改会通过 API 自动保存，模型密钥不会下发到浏览器。</span></div></aside>;}
