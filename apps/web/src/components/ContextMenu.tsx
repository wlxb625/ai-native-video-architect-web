import{BookOpen,Clapperboard,Image,Sparkles,StickyNote,UserRound}from'lucide-react';const items=[['story','剧情节点',Clapperboard],['character','角色节点',UserRound],['reference','资料节点',BookOpen],['asset','资产节点',Image],['generation','生成任务',Sparkles],['note','便签',StickyNote]]as const;export function ContextMenu({x,y,onAdd,onGenerate,onClose}:{x:number;y:number;onAdd:(type:string)=>void;onGenerate:()=>void;onClose:()=>void}){return <><button className="context-menu-backdrop" aria-label="关闭菜单" onClick={onClose}/><div className="context-menu" style={{left:x,top:y}}><div className="context-title">在此处创建</div>{items.map(([type,label,Icon])=><button key={type} onClick={()=>onAdd(type==='note'?'reference':type)}><Icon size={15}/><span>{label}</span></button>)}<div className="context-separator"/><button className="context-primary" onClick={onGenerate}><Sparkles size={15}/><span>生成下一步</span><kbd>G</kbd></button></div></>;}
