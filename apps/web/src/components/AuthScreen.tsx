import{useState,type FormEvent}from'react';import{Film,LockKeyhole,Sparkles}from'lucide-react';import{api,setAccessToken}from'../api';export function AuthScreen({onAuthenticated,onDemo}:{onAuthenticated:(user:any)=>void;onDemo:()=>void}){const[mode,setMode]=useState<'login'|'register'>('login');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[name,setName]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{const result=mode==='login'?await api.login(email,password):await api.register(email,password,name);setAccessToken(result.accessToken);onAuthenticated(result.user);}catch(err:any){setError(err.body?.error==='INVALID_CREDENTIALS'?'邮箱或密码不正确':err.message??'登录失败');}finally{setBusy(false);}};return <div className="auth-page"><div className="auth-ambient auth-a"/><div className="auth-ambient auth-b"/><section className="auth-story"><div className="auth-brand"><span><Film size={19}/></span>CineWeave <b>Graph Studio</b></div><div className="auth-copy"><small>GRAPH-NATIVE CREATION</small><h1>让创作过程<br/>成为可以执行的图。</h1><p>剧情、角色、资料、资产和模型任务不再散落在不同工具里。每一步都有数据契约、运行记录和可恢复状态。</p><div className="auth-points"><div><Sparkles size={16}/><span><b>GraphEngineering 编排</b><small>并行、重试、超时与验证</small></span></div><div><LockKeyhole size={16}/><span><b>服务端安全存储</b><small>权限、审计与密钥加密</small></span></div></div></div></section><section className="auth-panel"><form onSubmit={submit}><div className="auth-tabs"><button type="button" className={mode==='login'?'active':''} onClick={()=>setMode('login')}>登录</button><button type="button" className={mode==='register'?'active':''} onClick={()=>setMode('register')}>创建账户</button></div><h2>{mode==='login'?'继续你的创作项目':'创建 CineWeave 工作区'}</h2><p>{mode==='login'?'画布数据会从服务器安全恢复。':'密码至少 10 位，模型密钥稍后在设置中配置。'}</p>{mode==='register'&&<label>显示名称<input value={name} onChange={e=>setName(e.target.value)} required placeholder="你的名字或工作室"/></label>}<label>邮箱<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="name@example.com"/></label><label>密码<input type="password" minLength={mode==='register'?10:1} value={password} onChange={e=>setPassword(e.target.value)} required placeholder="••••••••••"/></label>{error&&<div className="auth-error">{error}</div>}<button className="auth-submit" disabled={busy}>{busy?'正在验证…':mode==='login'?'安全登录':'创建并进入'}</button><button className="auth-demo" type="button" onClick={onDemo}>先查看交互演示</button><div className="auth-foot">刷新令牌使用 HttpOnly Cookie；模型 API Key 不会传到浏览器。</div></form></section></div>;}
