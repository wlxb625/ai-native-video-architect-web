import type{FastifyInstance}from'fastify';import{nextStepRunSchema}from'@cineweave/contracts';import{requireAuth,assertProjectOwner}from'../auth.js';import{db}from'../db.js';import{audit}from'../audit.js';export async function runRoutes(app:FastifyInstance){app.addHook('preHandler',requireAuth);app.post('/projects/:projectId/runs/next-step',async(request:any,reply)=>{const{projectId}=request.params;await assertProjectOwner(request.authUser.id,projectId);const input=nextStepRunSchema.parse(request.body);const result=await db.query("INSERT INTO graph_runs(project_id,requested_by,graph_name,graph_version,input) VALUES($1,$2,'cineweave-next-step','1.0.0',$3) RETURNING id,status,created_at",[projectId,request.authUser.id,{projectId,...input}]);await audit({actorUserId:request.authUser.id,projectId,action:'graph_run.queued',targetType:'graph_run',targetId:result.rows[0].id,ip:request.ip,metadata:{sourceNodeId:input.sourceNodeId}});return reply.code(202).send({run:result.rows[0]});});app.get('/runs/:runId',async(request:any,reply)=>{const result=await db.query('SELECT gr.* FROM graph_runs gr JOIN projects p ON p.id=gr.project_id WHERE gr.id=$1 AND p.owner_id=$2',[request.params.runId,request.authUser.id]);if(!result.rows[0])return reply.code(404).send({error:'RUN_NOT_FOUND'});return{run:result.rows[0]};});app.get('/runs/:runId/events',async(request:any,reply)=>{const runResult=await db.query('SELECT gr.id FROM graph_runs gr JOIN projects p ON p.id=gr.project_id WHERE gr.id=$1 AND p.owner_id=$2',[request.params.runId,request.authUser.id]);if(!runResult.rows[0])return reply.code(404).send({error:'RUN_NOT_FOUND'});reply.hijack();reply.raw.writeHead(200,{'content-type':'text/event-stream','cache-control':'no-cache','connection':'keep-alive','x-accel-buffering':'no'});let sequence=Number(request.query?.after??0),closed=false;request.raw.on('close',()=>{closed=true;});const timer=setInterval(async()=>{if(closed)return clearInterval(timer);const events=await db.query('SELECT sequence,event_type,node_id,payload,created_at FROM graph_run_events WHERE run_id=$1 AND sequence>$2 ORDER BY sequence LIMIT 100',[request.params.runId,sequence]);for(const event of events.rows){sequence=Number(event.sequence);reply.raw.write(`id: ${sequence}\nevent: ${event.event_type}\ndata: ${JSON.stringify(event)}\n\n`);}const status=await db.query('SELECT status FROM graph_runs WHERE id=$1',[request.params.runId]);if(['succeeded','failed','cancelled'].includes(status.rows[0]?.status)){reply.raw.write(`event: done\ndata: ${JSON.stringify({status:status.rows[0].status})}\n\n`);clearInterval(timer);reply.raw.end();}},750);});}
