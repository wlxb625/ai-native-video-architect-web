name: ci
on: [push, pull_request]
jobs:
  verify:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env: {POSTGRES_DB: cineweave, POSTGRES_USER: cineweave, POSTGRES_PASSWORD: cineweave}
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U cineweave" --health-interval 5s --health-timeout 5s --health-retries 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: 22}
      - run: npm install
      - run: npm run graph:validate
      - run: npm run typecheck
      - run: npm run build
